<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TheCalendar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Ensure the status bar matches the toolbar/tab color -->
        <item name="android:statusBarColor">@color/purpledefault</item>
    </style>

    <style name="Theme.TheCalendar" parent="Base.Theme.TheCalendar" />

    <style name="Theme.TheCalendar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.TheCalendar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TheCalendar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>